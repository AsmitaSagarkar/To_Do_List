<%- include("header") -%>

    <!-- <% if(kindOfDay === "Sunday" || kindOfDay ==="Saturady"){ %>
        <h1 style="background-color: yellow;">It's a <%= kindOfDay %></h1>
    <%}else{%>
        
        <h1 style="background-color: orange;">It's a <%= kindOfDay %></h1>
    <%}%> -->
    <div class="box" id="heading">
        <h1>
            <%= kindOfDay %>
        </h1>
    </div>

    <div class="box">

        <% newListItem.forEach(function(item){%>
            <form action="/delete" method="post">

                <div class="item">
                    <input type="checkbox" name="checkbox" value="<%=item._id%>" onChange="this.form.submit()">
                    <p>
                        <%= item.name %>
                    </p>
                </div>
                <input type="hidden" name="listName" value="<%=kindOfDay %>"></input>
            </form>
            <%})%>


                <!-- <%for(var i=0 ; i<newListItem.length ; i++){%>
            <div class="item">
                <input type="checkbox">
                <p>
                    <%= newListItem[i].name %>
                </p>
            </div>
            <%}%> -->


                <form action="/" method="post" class="item">
                    <input type="text" name="newText" placeholder="Add items" autocomplete="off">
                    <button type="submit" name="list" value="<%=kindOfDay %>" >+</button>
                </form>
    </div>
    <%-include("footer") -%>